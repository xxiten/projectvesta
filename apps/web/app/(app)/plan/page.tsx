'use client';

import { RackScreen } from '@/features/room-rack/rack-screen';

/** The room rack — the primary operational surface (post-login landing). */
export default function PlanPage() {
  return <RackScreen />;
}
